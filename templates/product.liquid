{%- assign g_sale = false -%}
{%- if product.selected_or_first_available_variant.compare_at_price > product.selected_or_first_available_variant.price -%}
{%- assign g_sale = true -%}
{%- endif -%}

{%- assign g_sold_out = true -%}
{%- if product.available  -%}
{%- assign g_sold_out = false -%}
{%- endif -%}

{%- assign g_new = false -%}
{%- for tag in product.tags -%}
{%- assign g_tag_handle = tag | handle -%}
{%- if g_tag_handle == 'new' -%}
{%- assign g_new = true -%}
{%- endif -%}
{%- endfor -%}

{%- assign g_hot = false -%}
{%- for tag in product.tags -%}
{%- assign g_tag_handle = tag | handle -%}
{%- if g_tag_handle == 'hot' -%}
{%- assign g_hot = true -%}
{%- endif -%}
{%- endfor -%}

{% case settings.style_products_variants %}
{% when '1' %}
{% assign class_products_variants = 'style-products-variants-1' %}
{% when '2' %}
{% assign class_products_variants = 'style-products-variants-2' %}
{% when '3' %}
{% assign class_products_variants = 'style-products-variants-3' %}
{% endcase %}

{% assign featured_image = product.selected_or_first_available_variant.featured_image | default: product.featured_image %}
{%- assign g_current_variant = product.selected_or_first_available_variant -%}

{% case settings.layout_product %}
{% when '1' %}
<div class="sing-product-page">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":false, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":4,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>    
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="breadcrumbs">
                    <div class="g-breadcrumb-content">
                      <div class="g-breadcrumb-wrap">
                        <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% if collection.title %}
                        <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% endif %}
                        <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                      </div>
                    </div>
                  </div>
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '2' %}
<div class="sing-product-page products-content-layout-2">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":true, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>   
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="breadcrumbs">
                    <div class="g-breadcrumb-content">
                      <div class="g-breadcrumb-wrap">
                        <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% if collection.title %}
                        <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% endif %}
                        <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                      </div>
                    </div>
                  </div>
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '3' %}
<div class="sing-product-page products-content-layout-3">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":true, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>   
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="breadcrumbs">
                    <div class="g-breadcrumb-content">
                      <div class="g-breadcrumb-wrap">
                        <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% if collection.title %}
                        <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% endif %}
                        <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                      </div>
                    </div>
                  </div>
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '4' %}
<div class="sing-product-page products-content-layout-4">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    {% for image in product.images %}
                    <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                    {% endfor %}
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                </div>   
              </div>

              <div id="sticked-block" class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="content-layout4-sticked">
                    <div class="breadcrumbs">
                      <div class="g-breadcrumb-content">
                        <div class="g-breadcrumb-wrap">
                          <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                          <div class="g-breadcrumb-item">
                            <div class="g-breadcrumb-item-sep">&#8226;</div>
                          </div>
                          {% if collection.title %}
                          <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                          <div class="g-breadcrumb-item">
                            <div class="g-breadcrumb-item-sep">&#8226;</div>
                          </div>
                          {% endif %}
                          <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                        </div>
                      </div>
                    </div>
                    <h1 class="product-name-normal">{{ product.title }}</h1>
                    {% if settings.display_product_reviews %}
                    <div class="ratings">
                      <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                    </div> 
                    {% endif %}
                    <div class="box-single-product">
                      <span class="price">
                        {% if g_current_variant.compare_at_price > g_current_variant.price %}
                        <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                        <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                        <span class="amount">
                          <span id="ProductPrice"
                                itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                            {{ g_current_variant.price | money }}
                          </span>
                          <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                        </span>
                        {% else %}
                        <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                        <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                        <span class="amount">
                          <span id="ProductPrice"
                                itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                            {{ g_current_variant.price | money }}
                          </span>
                          <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                        </span>
                        {% endif %}
                      </span>
                      {% if product.compare_at_price > product.price %}
                      <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                      {% endif %}
                      <div class="top-description">
                        <ul class="nav nav-tabs">
                          {%- if product.metafields.meta.infoguide -%}
                          <li class="active">
                            <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                          </li>
                          {%- endif -%}
                          {%- if product.metafields.meta.shipping -%}
                          <li>
                            <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                          </li>
                          {%- endif -%}
                          {%- if product.metafields.meta.return -%}
                          <li>
                            <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                          </li>
                          {%- endif -%}
                        </ul>
                        <div class="tab-content">
                          {%- if product.metafields.meta.infoguide -%}
                          <div class="tab-pane active" id="info-guide">
                            {{ product.metafields.meta.infoguide }}
                          </div>
                          {%- endif -%}
                          {%- if product.metafields.meta.shipping -%}
                          <div class="tab-pane" id="shipping">
                            {{ product.metafields.meta.shipping }}
                          </div>
                          {%- endif -%}
                          {%- if product.metafields.meta.return -%}
                          <div class="tab-pane" id="return">
                            {{ product.metafields.meta.return }}
                          </div>
                          {%- endif -%}
                        </div>
                      </div>
                      {% include 'product-limited' %}
                      <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                        {% case settings.style_products_variants %}
                        {% assign hide_default_title = false %}
                        {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                        {% assign hide_default_title = true %}
                        {% endif %}
                        {% when '1' %}
                        <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% if product.variants.size > 1 %}
                        <div class="variations">
                          <div class="product-attribute">
                            {% for option in product.options %}
                            {% include 'swatch' with option %}
                            {% endfor %}
                          </div>
                        </div>
                        {% endif %}
                        {% when '2' %}
                        <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% when '3' %}
                        <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% if product.variants.size > 1 %}
                        <div class="variations variations-style-3">
                          <div class="product-attribute">
                            {% for option in product.options %}
                            {% include 'swatch' with option %}
                            {% endfor %}
                          </div>
                        </div>
                        {% endif %}
                        {% endcase %}
                        <ul class="info-single-product">
                          {% if settings.display_product_availability %}
                          <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                          {% endif %}
                          {% if settings.display_product_sku %}
                          {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                          {% endif %}
                          {% if settings.display_product_categories %}
                          {%- if product.collections.size > 0 -%}
                          <li>
                            <label>Categories:</label>
                            <div class="right-label">
                              {%- for collection in product.collections limit:5 -%}
                              {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                              {%- endfor -%}
                            </div>
                          </li>
                          {%- endif -%}
                          {% endif %}
                          {% if settings.display_product_tags %}
                          {%- if product.tags.size > 0 -%}
                          <li>
                            <label>Tags:</label>
                            <div class="right-label">
                              {%- for tag in product.tags limit:5 -%}
                              <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                              {%- endfor -%}
                            </div>
                          </li>
                          {%- endif -%}
                          {% endif %}
                        </ul>
                        <div class="products-info clearfix">
                          <div class="single_variation_wrap">
                            <div class="quantity">
                              <span class="qty-minus">-</span>
                              <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                              <span class="qty-plus">+</span>
                            </div>
                            <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                              {% unless g_current_variant.available %}
                              {{ 'products.product.sold_out' | t }}
                              {% else %}
                              {{ 'products.product.add_to_cart' | t }}
                              {% endunless %}
                            </button>
                            {% if settings.show_wishlist %}
                            {% include 'globo.wishlist.button' %}
                            {% endif %}
                            {% if settings.show_compare %}
                            {% include 'globo.compare.button' %}
                            {% endif %}
                          </div>
                        </div>
                      </form>
                      {% if settings.img_trust_badge != blank %}
                      <div class="trust-badge-img">
                        <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                      </div>
                      {% endif %}
                      {% include 'social-sharing' %}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{{ 'sticky-sidebar.js' | asset_url | script_tag }}
<script type="text/javascript">
  var stickySidebar = new StickySidebar('#sticked-block', {
    topSpacing: 120,
    bottomSpacing: 0,
    containerSelector: '.products-content-layout-4 .top-single-product-page .row',
    innerWrapperSelector: '.products-content-layout-4 .max-widht-470 .content-layout4-sticked'
  });
</script>
{% when '5' %}
<div class="sing-product-page products-content-layout-5">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    {% for image in product.images %}
                    <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                    {% endfor %}
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                </div>   
              </div>

              <div id="sticked-block" class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="content-layout4-sticked">
                    <div class="breadcrumbs">
                      <div class="g-breadcrumb-content">
                        <div class="g-breadcrumb-wrap">
                          <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                          <div class="g-breadcrumb-item">
                            <div class="g-breadcrumb-item-sep">&#8226;</div>
                          </div>
                          {% if collection.title %}
                          <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                          <div class="g-breadcrumb-item">
                            <div class="g-breadcrumb-item-sep">&#8226;</div>
                          </div>
                          {% endif %}
                          <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                        </div>
                      </div>
                    </div>
                    <h1 class="product-name-normal">{{ product.title }}</h1>
                    {% if settings.display_product_reviews %}
                    <div class="ratings">
                      <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                    </div> 
                    {% endif %}
                    <div class="box-single-product">
                      <span class="price">
                        {% if g_current_variant.compare_at_price > g_current_variant.price %}
                        <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                        <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                        <span class="amount">
                          <span id="ProductPrice"
                                itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                            {{ g_current_variant.price | money }}
                          </span>
                          <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                        </span>
                        {% else %}
                        <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                        <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                        <span class="amount">
                          <span id="ProductPrice"
                                itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                            {{ g_current_variant.price | money }}
                          </span>
                          <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                        </span>
                        {% endif %}
                      </span>
                      {% if product.compare_at_price > product.price %}
                      <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                      {% endif %}
                      <div class="top-description">
                        <ul class="nav nav-tabs">
                          {%- if product.metafields.meta.infoguide -%}
                          <li class="active">
                            <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                          </li>
                          {%- endif -%}
                          {%- if product.metafields.meta.shipping -%}
                          <li>
                            <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                          </li>
                          {%- endif -%}
                          {%- if product.metafields.meta.return -%}
                          <li>
                            <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                          </li>
                          {%- endif -%}
                        </ul>
                        <div class="tab-content">
                          {%- if product.metafields.meta.infoguide -%}
                          <div class="tab-pane active" id="info-guide">
                            {{ product.metafields.meta.infoguide }}
                          </div>
                          {%- endif -%}
                          {%- if product.metafields.meta.shipping -%}
                          <div class="tab-pane" id="shipping">
                            {{ product.metafields.meta.shipping }}
                          </div>
                          {%- endif -%}
                          {%- if product.metafields.meta.return -%}
                          <div class="tab-pane" id="return">
                            {{ product.metafields.meta.return }}
                          </div>
                          {%- endif -%}
                        </div>
                      </div>
                      {% include 'product-limited' %}
                      <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                        {% case settings.style_products_variants %}
                        {% assign hide_default_title = false %}
                        {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                        {% assign hide_default_title = true %}
                        {% endif %}
                        {% when '1' %}
                        <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% if product.variants.size > 1 %}
                        <div class="variations">
                          <div class="product-attribute">
                            {% for option in product.options %}
                            {% include 'swatch' with option %}
                            {% endfor %}
                          </div>
                        </div>
                        {% endif %}
                        {% when '2' %}
                        <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% when '3' %}
                        <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                          <select id="product-select-{{ product.id }}" name="id">
                            {% for variant in product.variants %}
                            <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                            {% endfor %}
                          </select>
                        </div>
                        {% if product.variants.size > 1 %}
                        <div class="variations variations-style-3">
                          <div class="product-attribute">
                            {% for option in product.options %}
                            {% include 'swatch' with option %}
                            {% endfor %}
                          </div>
                        </div>
                        {% endif %}
                        {% endcase %}
                        <ul class="info-single-product">
                          {% if settings.display_product_availability %}
                          <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                          {% endif %}
                          {% if settings.display_product_sku %}
                          {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                          {% endif %}
                          {% if settings.display_product_categories %}
                          {%- if product.collections.size > 0 -%}
                          <li>
                            <label>Categories:</label>
                            <div class="right-label">
                              {%- for collection in product.collections limit:5 -%}
                              {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                              {%- endfor -%}
                            </div>
                          </li>
                          {%- endif -%}
                          {% endif %}
                          {% if settings.display_product_tags %}
                          {%- if product.tags.size > 0 -%}
                          <li>
                            <label>Tags:</label>
                            <div class="right-label">
                              {%- for tag in product.tags limit:5 -%}
                              <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                              {%- endfor -%}
                            </div>
                          </li>
                          {%- endif -%}
                          {% endif %}
                        </ul>
                        <div class="products-info clearfix">
                          <div class="single_variation_wrap">
                            <div class="quantity">
                              <span class="qty-minus">-</span>
                              <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                              <span class="qty-plus">+</span>
                            </div>
                            <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                              {% unless g_current_variant.available %}
                              {{ 'products.product.sold_out' | t }}
                              {% else %}
                              {{ 'products.product.add_to_cart' | t }}
                              {% endunless %}
                            </button>
                            {% if settings.show_wishlist %}
                            {% include 'globo.wishlist.button' %}
                            {% endif %}
                            {% if settings.show_compare %}
                            {% include 'globo.compare.button' %}
                            {% endif %}
                          </div>
                        </div>
                      </form>
                      {% if settings.img_trust_badge != blank %}
                      <div class="trust-badge-img">
                        <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                      </div>
                      {% endif %}
                      {% include 'social-sharing' %}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{{ 'sticky-sidebar.js' | asset_url | script_tag }}
<script type="text/javascript">
  var stickySidebar = new StickySidebar('#sticked-block', {
    topSpacing: 120,
    bottomSpacing: 0,
    containerSelector: '.products-content-layout-5 .top-single-product-page .row',
    innerWrapperSelector: '.products-content-layout-5 .max-widht-470 .content-layout4-sticked'
  });
</script>
{% when '6' %}
<div class="sing-product-page products-content-layout-6">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":true,"dots":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav","nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>"}'>
                      {% for image in product.images %}
                      <div class="image-large"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" style="display: none;" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":true, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>    
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <div class="breadcrumbs">
                    <div class="g-breadcrumb-content">
                      <div class="g-breadcrumb-wrap">
                        <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% if collection.title %}
                        <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                        <div class="g-breadcrumb-item">
                          <div class="g-breadcrumb-item-sep">&#8226;</div>
                        </div>
                        {% endif %}
                        <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                      </div>
                    </div>
                  </div>
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '7' %}
<div class="sing-product-page products-content-layout-7">
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-12 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":false, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":4,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>    
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-detail-content text-center">
      <div class="container">
        <div class="breadcrumbs">
          <div class="g-breadcrumb-content">
            <div class="g-breadcrumb-wrap">
              <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
              <div class="g-breadcrumb-item">
                <div class="g-breadcrumb-item-sep">&#8226;</div>
              </div>
              {% if collection.title %}
              <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
              <div class="g-breadcrumb-item">
                <div class="g-breadcrumb-item-sep">&#8226;</div>
              </div>
              {% endif %}
              <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
            </div>
          </div>
        </div>
        <h1 class="product-name-normal">{{ product.title }}</h1>
        {% if settings.display_product_reviews %}
        <div class="ratings">
          <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
        </div> 
        {% endif %}
        <div class="box-single-product">
          <span class="price">
            {% if g_current_variant.compare_at_price > g_current_variant.price %}
            <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
            <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
            <span class="amount">
              <span id="ProductPrice"
                    itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                {{ g_current_variant.price | money }}
              </span>
              <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
            </span>
            {% else %}
            <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
            <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
            <span class="amount">
              <span id="ProductPrice"
                    itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                {{ g_current_variant.price | money }}
              </span>
              <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
            </span>
            {% endif %}
          </span>
          {% if product.compare_at_price > product.price %}
          <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
          {% endif %}
          <div class="top-description">
            <ul class="nav nav-tabs">
              {%- if product.metafields.meta.infoguide -%}
              <li class="active">
                <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
              </li>
              {%- endif -%}
              {%- if product.metafields.meta.shipping -%}
              <li>
                <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
              </li>
              {%- endif -%}
              {%- if product.metafields.meta.return -%}
              <li>
                <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
              </li>
              {%- endif -%}
            </ul>
            <div class="tab-content">
              {%- if product.metafields.meta.infoguide -%}
              <div class="tab-pane active" id="info-guide">
                {{ product.metafields.meta.infoguide }}
              </div>
              {%- endif -%}
              {%- if product.metafields.meta.shipping -%}
              <div class="tab-pane" id="shipping">
                {{ product.metafields.meta.shipping }}
              </div>
              {%- endif -%}
              {%- if product.metafields.meta.return -%}
              <div class="tab-pane" id="return">
                {{ product.metafields.meta.return }}
              </div>
              {%- endif -%}
            </div>
          </div>
          {% include 'product-limited' %}
          <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
            {% case settings.style_products_variants %}
            {% assign hide_default_title = false %}
            {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
            {% assign hide_default_title = true %}
            {% endif %}
            {% when '1' %}
            <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
              <select id="product-select-{{ product.id }}" name="id">
                {% for variant in product.variants %}
                <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                {% endfor %}
              </select>
            </div>
            {% if product.variants.size > 1 %}
            <div class="variations">
              <div class="product-attribute">
                {% for option in product.options %}
                {% include 'swatch' with option %}
                {% endfor %}
              </div>
            </div>
            {% endif %}
            {% when '2' %}
            <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
              <select id="product-select-{{ product.id }}" name="id">
                {% for variant in product.variants %}
                <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                {% endfor %}
              </select>
            </div>
            {% when '3' %}
            <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
              <select id="product-select-{{ product.id }}" name="id">
                {% for variant in product.variants %}
                <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                {% endfor %}
              </select>
            </div>
            {% if product.variants.size > 1 %}
            <div class="variations variations-style-3">
              <div class="product-attribute">
                {% for option in product.options %}
                {% include 'swatch' with option %}
                {% endfor %}
              </div>
            </div>
            {% endif %}
            {% endcase %}
            <ul class="info-single-product">
              {% if settings.display_product_availability %}
              <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
              {% endif %}
              {% if settings.display_product_sku %}
              {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
              {% endif %}
              {% if settings.display_product_categories %}
              {%- if product.collections.size > 0 -%}
              <li>
                <label>Categories:</label>
                <div class="right-label">
                  {%- for collection in product.collections limit:5 -%}
                  {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                  {%- endfor -%}
                </div>
              </li>
              {%- endif -%}
              {% endif %}
              {% if settings.display_product_tags %}
              {%- if product.tags.size > 0 -%}
              <li>
                <label>Tags:</label>
                <div class="right-label">
                  {%- for tag in product.tags limit:5 -%}
                  <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                  {%- endfor -%}
                </div>
              </li>
              {%- endif -%}
              {% endif %}
            </ul>
            <div class="products-info clearfix">
              <div class="single_variation_wrap">
                <div class="quantity">
                  <span class="qty-minus">-</span>
                  <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                  <span class="qty-plus">+</span>
                </div>
                <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                  {% unless g_current_variant.available %}
                  {{ 'products.product.sold_out' | t }}
                  {% else %}
                  {{ 'products.product.add_to_cart' | t }}
                  {% endunless %}
                </button>
                {% if settings.show_wishlist %}
                {% include 'globo.wishlist.button' %}
                {% endif %}
                {% if settings.show_compare %}
                {% include 'globo.compare.button' %}
                {% endif %}
              </div>
            </div>
          </form>
          {% if settings.img_trust_badge != blank %}
          <div class="trust-badge-img">
            <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
          </div>
          {% endif %}
          {% include 'social-sharing' %}
        </div>
      </div>
  	</div>
  	<div class="product-description-layout7">
  		{{ product.description }}
  	</div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          {% if settings.display_additional_tab %}
          <li class="active">
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          {% if settings.display_additional_tab %}
          <div class="tab-pane active" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '8' %}
<div class="sing-product-page products-content-layout-2 products-content-layout-8">
    <div class="vc_row wpb_row vc_row-fluid section-page-header" {% if collection.image %}style="background-image: url({{ collection.image | img_url: 'master' }});"{% endif %}>
      <div class="container">
        <div class="page-header-inner wow fadeIn">
          <div class="row">
            <div class="col-xs-12">
              <header>
                <h1>{{ collection.title }}</h1>
              </header>
              <div class="breadcrumbs hidden-xs">
                <div class="g-breadcrumb-content">
                  <div class="g-breadcrumb-wrap">
                    <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                    <div class="g-breadcrumb-item">
                      <div class="g-breadcrumb-item-sep">&#8226;</div>
                    </div>
                    {% if collection.title %}
                    <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                    <div class="g-breadcrumb-item">
                      <div class="g-breadcrumb-item-sep">&#8226;</div>
                    </div>
                    {% endif %}
                    <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":true, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>   
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% when '9' %}
<div class="sing-product-page products-content-layout-3 products-content-layout-8">
    <div class="vc_row wpb_row vc_row-fluid section-page-header" {% if collection.image %}style="background-image: url({{ collection.image | img_url: 'master' }});"{% endif %}>
      <div class="container">
        <div class="page-header-inner wow fadeIn">
          <div class="row">
            <div class="col-xs-12">
              <header>
                <h1>{{ collection.title }}</h1>
              </header>
              <div class="breadcrumbs hidden-xs">
                <div class="g-breadcrumb-content">
                  <div class="g-breadcrumb-wrap">
                    <div class="g-breadcrumb-item"><a href="{{ shop.url }}" class="g-breadcrumb-item-link is-home" rel="home" title="{{ shop.name | escape }}">{{ 'general.breadcrumbs.home' | t }}</a></div>
                    <div class="g-breadcrumb-item">
                      <div class="g-breadcrumb-item-sep">&#8226;</div>
                    </div>
                    {% if collection.title %}
                    <div class="g-breadcrumb-item"><a href="{{ collection.url }}"><span class="g-breadcrumb-item-target">{{ collection.title }}</span></a></div>
                    <div class="g-breadcrumb-item">
                      <div class="g-breadcrumb-item-sep">&#8226;</div>
                    </div>
                    {% endif %}
                    <div class="g-breadcrumb-item"><span class="g-breadcrumb-item-target">{{ product.title }}</span></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="products-content">
      	<div class="top-single-product-page">
          <div class="{% if settings.use_container_top_single %}container{% else %}container-full{% endif %}">
            <div class="row">
              <div class="col-sm-6 col-xs-12">
                <div class="single_image-product">
                  <div class="featured_image-product">
                    <div style="display: none;">{% include 'dlicon-arrow-left2' %}{% include 'dlicon-arrow-right2' %}</div>
                    <div class="slider-for" data-slick='{"infinite":false,"dots":false,"slidesToShow":1,"slidesToScroll":1,"autoplay":false,"arrows":false,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"asNavFor":".slider-nav"}'>
                      {% for image in product.images %}
                      <div class="image-large">{% if settings.enable_zoom %}<div class="g-image-zoom">{% endif %}<img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" />{% if settings.enable_zoom %}</div>{% endif %}{% if settings.display_product_popup %}<a data-fancybox="gallery" href="{{ image | img_url: 'master' }}"><i class="fa fa-search-plus"></i></a>{% endif %}</div>
                      {% endfor %}
                    </div>
                    {% if g_sale  %}
                    <span class="label-product sale">{{ 'products.product.on_sale' | t }}</span>
                    {% elsif g_hot %}
                    <span class="label-product hot">{{ 'products.product.hot' | t }}</span>
                    {% elsif g_sold_out  %}
                    <span class="label-product sold-out">{{ 'products.product.sold_out' | t }}</span>
                    {% elsif g_new  %}
                    <span class="label-product new">{{ 'products.product.new' | t }}</span>
                    {% endif %}
                    {%- if product.metafields.meta.video -%}
                    <a class="video_popup" href="{{ product.metafields.meta.video }}" rel="nofollow" tabindex="0"><i class="dl-icon-format-video"></i></a>
                    {%- endif -%}
                  </div>
                  {% if product.images.size > 1 %}
                  <div class="slider-nav" data-slick='{"infinite":false,"dots":false,"slidesToShow":4,"slidesToScroll":1,"autoplay":false,"arrows":true,"speed":"300","autoplaySpeed":3000,"centerMode":false,"variableWidth":false,"focusOnSelect": true,"vertical":true, "nextArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-next default\"><svg><use xlink:href=\"#dlicon-arrow-right2\"><\/use><\/svg><\/button>","prevArrow":"<button type=\"button\" style=\"font-size:24px;width:24px;height:24px;line-height:24px;\" class=\"slick-prev default\"><svg><use xlink:href=\"#dlicon-arrow-left2\"><\/use><\/svg><\/button>", "asNavFor":".slider-for", "responsive":[{"breakpoint":1200,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":992,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":768,"settings":{"slidesToShow":3,"slidesToScroll":1}},{"breakpoint":480,"settings":{"slidesToShow":2,"slidesToScroll":1}}]}'>
                    {% for image in product.images %}
                    <div class="image-thumb"><img src="{{ image | img_url: 'master' }}" alt="{{ image.alt | escape }}" data-image-id="{{ image.id }}" /></div>
                    {% endfor %}
                  </div>
                  {% endif %}
                </div>   
              </div>

              <div class="col-sm-6 col-xs-12 padding-left-55">
                <div class="max-widht-470">
                  <h1 class="product-name-normal">{{ product.title }}</h1>
                  {% if settings.display_product_reviews %}
                  <div class="ratings">
                    <span class="shopify-product-reviews-badge" data-id="{{ product.id }}"></span>
                  </div> 
                  {% endif %}
                  <div class="box-single-product">
                    <span class="price">
                      {% if g_current_variant.compare_at_price > g_current_variant.price %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% else %}
                      <span class="hidden">{{ 'products.product.regular_price' | t }}</span>
                      <del id="ComparePrice" class="hide">{{ g_current_variant.compare_at_price | money }}</del>
                      <span class="amount">
                        <span id="ProductPrice"
                              itemprop="price" content="{{ g_current_variant.price | divided_by: 100.00 }}">
                          {{ g_current_variant.price | money }}
                        </span>
                        <span class="product-price__sale-label hide">{{ 'products.product.on_sale' | t }}</span>
                      </span>
                      {% endif %}
                    </span>
                    {% if product.compare_at_price > product.price %}
                    <span class="you-save">{{ 'products.product.you_save' | t }} {{ product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round }}% </span>
                    {% endif %}
                    <div class="top-description">
                      <ul class="nav nav-tabs">
                        {%- if product.metafields.meta.infoguide -%}
                        <li class="active">
                          <a href="#info-guide" data-toggle="tab">{{ 'products.product.info_guide' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <li>
                          <a href="#shipping" data-toggle="tab">{{ 'products.product.shipping' | t }}</a>
                        </li>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <li>
                          <a href="#return" data-toggle="tab">{{ 'products.product.return' | t }}</a>
                        </li>
                        {%- endif -%}
                      </ul>
                      <div class="tab-content">
                        {%- if product.metafields.meta.infoguide -%}
                        <div class="tab-pane active" id="info-guide">
                          {{ product.metafields.meta.infoguide }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.shipping -%}
                        <div class="tab-pane" id="shipping">
                          {{ product.metafields.meta.shipping }}
                        </div>
                        {%- endif -%}
                        {%- if product.metafields.meta.return -%}
                        <div class="tab-pane" id="return">
                          {{ product.metafields.meta.return }}
                        </div>
                        {%- endif -%}
                      </div>
                    </div>
                    {% include 'product-limited' %}
                    <form action="/cart/add" method="post" enctype="multipart/form-data" class="variations_form cart {{ class_products_variants }}">
                      {% case settings.style_products_variants %}
                      {% assign hide_default_title = false %}
                      {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
                      {% assign hide_default_title = true %}
                      {% endif %}
                      {% when '1' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% when '2' %}
                      <div class="select clearfix variations-style-2" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% when '3' %}
                      <div class="select clearfix" {% if hide_default_title %} style="display:none"{% endif %}>
                        <select id="product-select-{{ product.id }}" name="id">
                          {% for variant in product.variants %}
                          <option value="{{ variant.id }}"{% if variant == product.selected_or_first_available_variant %} selected="selected" {% endif %}>{{ variant.title }}</option>
                          {% endfor %}
                        </select>
                      </div>
                      {% if product.variants.size > 1 %}
                      <div class="variations variations-style-3">
                        <div class="product-attribute">
                          {% for option in product.options %}
                          {% include 'swatch' with option %}
                          {% endfor %}
                        </div>
                      </div>
                      {% endif %}
                      {% endcase %}
                      <ul class="info-single-product">
                        {% if settings.display_product_availability %}
                        <li class="availability"><label>{{ 'products.product.availability' | t }}:</label> <span>{% if product.available %}{{ 'products.product.in_stock' | t }}{% else %}{{ 'products.product.out_of_stock' | t }}{% endif %}</span></li>
                        {% endif %}
                        {% if settings.display_product_sku %}
                        {% if g_current_variant.sku != blank %}<li><label>{{ 'products.product.sku' | t }}:</label><span class="variant-sku">{{g_current_variant.sku}}</span></li>{% endif %}
                        {% endif %}
                        {% if settings.display_product_categories %}
                        {%- if product.collections.size > 0 -%}
                        <li>
                          <label>Categories:</label>
                          <div class="right-label">
                            {%- for collection in product.collections limit:5 -%}
                            {{ collection.title | link_to: collection.url }}{%- unless forloop.last == true -%},{%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                        {% if settings.display_product_tags %}
                        {%- if product.tags.size > 0 -%}
                        <li>
                          <label>Tags:</label>
                          <div class="right-label">
                            {%- for tag in product.tags limit:5 -%}
                            <a href="{%- if collection != blank -%}{{ collection.url }}{%- else -%}{{ product.collections.first.url }}{%- endif -%}/{{ tag | handle }}">{{ tag | remove:'type ' | remove:'Type ' }}</a>{%- unless forloop.last -%}, {%- endunless -%}
                            {%- endfor -%}
                          </div>
                        </li>
                        {%- endif -%}
                        {% endif %}
                      </ul>
                      <div class="products-info clearfix">
                        <div class="single_variation_wrap">
                          <div class="quantity">
                            <span class="qty-minus">-</span>
                            <input type="number" class="input-text qty text" step="1" min="1" max="" name="quantity" value="1" title="Qty" size="4" pattern="[0-9]*" inputmode="numeric" />
                            <span class="qty-plus">+</span>
                          </div>
                          <button type="submit" {% unless g_current_variant.available %}disabled="disabled"{% endunless %} class="single_add_to_cart_button {% if settings.enable_ajax %}ajax_form_cart{% endif %} button alt">
                            {% unless g_current_variant.available %}
                            {{ 'products.product.sold_out' | t }}
                            {% else %}
                            {{ 'products.product.add_to_cart' | t }}
                            {% endunless %}
                          </button>
                          {% if settings.show_wishlist %}
                          {% include 'globo.wishlist.button' %}
                          {% endif %}
                          {% if settings.show_compare %}
                          {% include 'globo.compare.button' %}
                          {% endif %}
                        </div>
                      </div>
                    </form>
                    {% if settings.img_trust_badge != blank %}
                    <div class="trust-badge-img">
                      <img src="{{ settings.img_trust_badge | img_url: 'master' }}" alt="{{ settings.img_trust_badge.alt | escape }}" />
                    </div>
                    {% endif %}
                    {% include 'social-sharing' %}
                  </div>
                </div>
              </div>
            </div>
          </div>       		
      	</div>
    </div>
  	<div class="product-tabs-info">
      <div class="container">
        <ul class="nav nav-tabs">
          <li class="active">
            <a href="#tab-description" data-toggle="tab">{{ settings.product_description_title }}</a>
          </li>
          {% if settings.display_additional_tab %}
          <li>
            <a href="#tab-additional" data-toggle="tab">{{ settings.additional_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_custom_tab %}
          <li>
            <a href="#tab-custom" data-toggle="tab">{{ settings.custom_tab_title }}</a>
          </li>
          {% endif %}
          {% if settings.display_size_chart %}
          <li>
            <a href="#tab-size-chart" data-toggle="tab">{{ settings.custom_size_chart_title }}</a>
          </li>          
          {% endif %}
          {% if settings.display_product_reviews %}
          <li>
            <a href="#tab-reviews" data-toggle="tab">{{ settings.product_review_title }}</a>
          </li>           
          {% endif %}
        </ul>
        <div class="tab-content">
          <div class="tab-pane active" id="tab-description">
            {{ product.description }}
          </div>
          {% if settings.display_additional_tab %}
          <div class="tab-pane" id="tab-additional">
            <table class="shop_attributes">
              <tbody>
                {% for option in product.options_with_values %}
                <tr class="{{ option.name }}">
                  <th>{{ option.name }}</th>
                  <td>
                    {%- for g_value in option.values -%}
                    {{ g_value }}{% unless forloop.last == true %}, {% endunless %}
                    {%- endfor -%}
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>  
          </div>
          {% endif %}
          {% if settings.display_custom_tab %}
          <div class="tab-pane" id="tab-custom">
            {{ settings.custom_tab_content }}
          </div>
          {% endif %}
          {% if settings.display_size_chart %}
          <div class="tab-pane" id="tab-size-chart">
            <img src="{{ 'custom_size_chart_content.jpg' | asset_url }}" alt="{{ settings.custom_size_chart_title }}" />
          </div>
          {% endif %}
          {% if settings.display_product_reviews %}
          <div class="tab-pane" id="tab-reviews">
            <div id="shopify-product-reviews" data-id="{{product.id}}">{{ product.metafields.spr.reviews }}</div>
          </div>          	
          {% endif %}
        </div>
      </div>
  	</div>
    {% if settings.show_related_products %}
    {% include 'related-product' %}
    {% endif %}
</div>
{% endcase %}
{% if settings.show_product_countdown_scroll %}
{% include 'product-countdown-scroll' %}
{% endif %}
{% unless product == empty %}
  <script type="application/json" id="ProductJson-{{ product.id }}">
    {{ product | json }}
  </script>
{% endunless %}

<script>
  // Override default values of shop.strings for each template.
  // Alternate product templates can change values of
  // add to cart button, sold out, and unavailable states here.
  theme.productStrings = {
    addToCart: {{ 'products.product.add_to_cart' | t | json }},
    soldOut: {{ 'products.product.sold_out' | t | json }},
    unavailable: {{ 'products.product.unavailable' | t | json }}
  }
</script>

{% assign current_variant = product.selected_or_first_available_variant %}

<script type="application/ld+json">
{
  "@context": "http://schema.org/",
  "@type": "Product",
  "name": "{{ product.title | escape }}",
  "url": "{{ shop.url }}{{ product.url }}",
  {%- if product.featured_image -%}
    {%- assign image_size = product.featured_image.width | append: 'x' -%}
    "image": [
      "https:{{ product.featured_image.src | img_url: image_size }}"
    ],
  {%- endif -%}
  "description": "{{ product.description | strip_html | escape }}",
  {%- if current_variant.sku != blank -%}
    "sku": "{{ current_variant.sku }}",
  {%- endif -%}
  "brand": {
    "@type": "Thing",
    "name": "{{ product.vendor | escape }}"
  },
  "offers": [
    {%- for variant in product.variants -%}
      {
        "@type" : "Offer",
        {%- if variant.sku != blank -%}
          "sku": "{{ variant.sku }}",
        {%- endif -%}
        "availability" : "http://schema.org/{% if product.available %}InStock{% else %}OutOfStock{% endif %}",
        "price" : "{{ variant.price | divided_by: 100.00 }}",
        "priceCurrency" : "{{ cart.currency.iso_code }}",
        "url" : "{{ shop.url }}{{ variant.url }}"
      }{% unless forloop.last %},{% endunless %}
    {%- endfor -%}
  ]
}
</script>


<script>
  var selectCallback = function(variant, selector) {
    if (variant) {
      jQuery('.variant-sku').text(variant.sku);
    }
    else {
      jQuery('.variant-sku').empty();
    }
    var featured_image_id = null;
    if(variant.featured_image) {
      featured_image_id = variant.featured_image.id;
      jQuery('.sing-product-page').addClass('featured_image').removeClass('not_featured_image');
    }else{
      featured_image_id = null;
      jQuery('.sing-product-page').addClass('not_featured_image').removeClass('featured_image');
    }
	if( jQuery('.sing-product-page').hasClass('featured_image')) {
      jQuery('.image-thumb').find(' img[data-image-id="'+featured_image_id+'"]').trigger('click');
    }
    if (variant && variant.available) {
      jQuery('.availability span').html('In stock');
      jQuery('.single_add_to_cart_button').removeAttr('disabled').removeClass('disabled');
      jQuery('.single_add_to_cart_button').html("{{ 'products.product.add_to_cart' | t }}");
      if(variant.price < variant.compare_at_price){
        jQuery('#ProductPrice').html(Shopify.formatMoney(variant.price, '{{ shop.money_format }}'));
        jQuery('#ComparePrice').html(Shopify.formatMoney(variant.compare_at_price, '{{ shop.money_format }}'));
      } else {
        jQuery('#ProductPrice').html(Shopify.formatMoney(variant.price, '{{ shop.money_format }}'));
      }
    } else {
      jQuery('.availability span').html('Out of stock');                                                   
      jQuery('.single_add_to_cart_button').addClass('disabled').attr('disabled', 'disabled');
      jQuery('.single_add_to_cart_button').html("{{ 'products.product.sold_out' | t }}");
      if(variant.price < variant.compare_at_price){
        jQuery('#ProductPrice').html(Shopify.formatMoney(variant.price, '{{ shop.money_format }}'));
        jQuery('#ComparePrice').html(Shopify.formatMoney(variant.compare_at_price, '{{ shop.money_format }}'));
      } else {
        jQuery('#ProductPrice').html(Shopify.formatMoney(variant.price, '{{ shop.money_format }}'));
      }
    }
    // BEGIN SWATCHES
    if (variant) {
      var form = jQuery('#' + selector.domIdPrefix).closest('form');
      for (var i=0,length=variant.options.length; i<length; i++) {
        var radioButton = form.find('.swatch[data-option-index="' + i + '"] :radio[value="' + variant.options[i] +'"]');
        if (radioButton.size()) {
          radioButton.get(0).checked = true;
        }
      }
    }
    // END SWATCHES
  };

  jQuery(document).ready(function(){
    new Shopify.OptionSelectors("product-select-{{product.id}}", { product: {{ product | json }}, onVariantSelected: selectCallback, enableHistoryState: true });
    {% if product.options.size == 1 and product.options.first != 'Title' %}
    jQuery('.selector-wrapper:eq(0)').prepend('<label>{{ product.options.first | escape }}</label>');
    {% endif %}
    {% if product.variants.size == 1 and product.variants.first.title contains 'Default' %}
    jQuery('.selector-wrapper').hide();
    {% endif %}                                          
    jQuery('.swatch :radio').change(function() {
      var optionIndex = jQuery(this).closest('.swatch').attr('data-option-index');
      var optionValue = jQuery(this).val();
      jQuery(this).closest('form').find('.single-option-selector').eq(optionIndex).val(optionValue).trigger('change');
    }); 
  });
</script>